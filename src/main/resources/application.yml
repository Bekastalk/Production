server:
    port: 9090
spring:
    datasource:
        password: root
        url: jdbc:mysql://localhost:3306/root
        username: root
    jpa:
        database-platform: org.hibernate.dialect.MySQLDialect
        hibernate:
            ddl-auto: create
        show-sql: true
